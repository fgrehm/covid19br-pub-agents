<p>
  Send WebHooks (POST requests) to this Agent at <%= link_to web_requests_url(:agent_id => @agent.id, :user_id => current_user.id, :secret => @agent.interpolated['secret']), web_requests_url(:agent_id => @agent.id, :user_id => current_user.id, :secret => @agent.interpolated['secret']), :target => :blank %>
</p>
